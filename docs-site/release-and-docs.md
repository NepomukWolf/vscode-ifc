# Release and Docs

Public documentation is deployed from the `production` branch.

Docs should describe released behavior only. When a feature changes, update the matching docs page in the same pull request that prepares the release branch.

The repository can keep `dev` as the active development branch while GitHub Pages publishes from `production`.

